import HomePage from "./components/Home"

function App() {
  return (
    <div>
      <HomePage />
    </div>
  )
}

export default App


// data.forEach(repo => {
//   console.log(`${repo.name} - ⭐ ${repo.stargazers_count} - ${repo.language}`);
// });